'use script';

const square = (number) => number * number;

// const hypotenuse = (a , b) => Math.sqrt(a**2 + b**2);

const abs = (number) => 
 { if (number >= 0) 
  return number;
  else {
    return -number;
  }
}       